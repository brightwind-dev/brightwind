from typing import List, Dict


def calc_shear(wind_speeds: List[float], heights: List[float]) -> float:
    """
    Calculate the shear using the power law method.
    :param wind_speeds: List of wind speeds.
    :param heights: List of heights. The position of the height in the list must be the same position in the list as its
    corresponding wind speed value.
    :return: The shear value alpha.
    """
    alpha = 0.2  # replace with the calculation code.
    return alpha
